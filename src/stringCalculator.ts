const add = (input:string): number =>{
    if (!input) return 0;
    return 1

}

const extractDelimiters = (input: string) : [] =>{
    //findout the delimiters and provide the array of numbers 
    const delimiters = [',', '\n',':', ';'];
    console.log(input);
    return [];
}